name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go-check:
    name: Go vet + test
    # Same-repo gate: fork PRs are skipped so secrets aren't exposed to
    # untrusted forks (forks are also disabled at the org level, so this
    # is closed-by-default in practice).
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: [self-hosted, self-hosted-hopnet-ci]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true
      - run: go vet ./...
      - run: go test ./... -race -count=1 -timeout=60s

  cross-build:
    name: Cross-compile (${{ matrix.goos }}/${{ matrix.goarch }})
    # `needs: [go-check]` already gates fork PRs; matrix jobs inherit
    # the gate transitively (skipped go-check skips dependents).
    runs-on: [self-hosted, self-hosted-hopnet-ci]
    needs: [go-check]
    strategy:
      matrix:
        include:
          - { goos: linux,  goarch: amd64 }
          - { goos: linux,  goarch: arm64 }
          - { goos: darwin, goarch: amd64 }
          - { goos: darwin, goarch: arm64 }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true
      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          go build -trimpath \
            -ldflags="-s -w -X main.version=${GITHUB_SHA::7} -X main.commit=${GITHUB_SHA::7}" \
            -o ./bin/hopnet-${{ matrix.goos }}-${{ matrix.goarch }} \
            ./cmd/hopnet
      - uses: actions/upload-artifact@v4
        with:
          name: hopnet-${{ matrix.goos }}-${{ matrix.goarch }}
          path: ./bin/hopnet-${{ matrix.goos }}-${{ matrix.goarch }}
          if-no-files-found: error
          retention-days: 7

  docker-build:
    name: Docker build (${{ matrix.platforms }})
    # Same `needs: [go-check]` transitive gate as cross-build.
    runs-on: [self-hosted, self-hosted-hopnet-ci]
    needs: [go-check]
    strategy:
      matrix:
        platforms:
          - ${{ github.event_name == 'pull_request' && 'linux/amd64' || 'linux/amd64,linux/arm64' }}
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
        if: contains(matrix.platforms, 'arm64')
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: ${{ matrix.platforms }}
          push: false
          cache-from: type=gha,scope=hopnet-cli
          cache-to: type=gha,mode=max,scope=hopnet-cli
